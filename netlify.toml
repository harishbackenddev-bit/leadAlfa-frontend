# netlify.toml
[build]
  command = "npm run build"
  publish = "dist"

# WordPress /wp-json and /wp-content proxies are generated at build time into
# public/_redirects from VITE_WP_SITE_URL (see scripts/generate-proxy-config.mjs).

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
